[build-system]
requires = ["maturin>=1.9.4,<2"]
build-backend = "maturin"

[tool.maturin]
module-name = "p5.rust._accelerated"
python-source = "../../src"
bindings = "pyo3"
features = ["extension-module"]
