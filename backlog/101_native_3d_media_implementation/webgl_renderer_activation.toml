[my_pbi]
title = "Activate WEBGL renderer selection and cross-backend 3D integration"
description = '''
As a p5-py user,
I want `create_canvas(..., renderer=WEBGL)` to select a stable 3D rendering path,
So that sketches can opt into advanced 3D behavior without breaking the existing 2D runtime.
'''
acceptance_criteria = '''
A WEBGL renderer-selection path exists without regressing current 2D backends. The current backends activate the shared software-projected 3D path with clear capability gating, deterministic headless behavior, and focused smoke/example coverage.
'''
priotity = 'high'
status = 'DONE'

[my_pbi.task_1]
order = 1
status = 'DONE'
description = '''
Add a renderer-selection path for `create_canvas(..., renderer=WEBGL)` and related backend selection logic without changing the default 2D behavior.
'''

[my_pbi.task_2]
order = 2
status = 'DONE'
description = '''
Move the native Pyglet depth-tested renderer spike into follow-on epic 102 so epic 101 can close around the shipped software WEBGL milestone.
'''

[my_pbi.task_3]
order = 3
status = 'DONE'
description = '''
Wire 3D capability checks into backend/runtime code so unsupported backends fail with clear package-specific errors instead of missing methods.
'''

[my_pbi.task_4]
order = 4
status = 'DONE'
description = '''
Add focused tests and smoke examples covering WEBGL renderer activation, capability gating, and the first software 3D frame.
'''
